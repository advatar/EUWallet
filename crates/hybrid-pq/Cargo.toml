[package]
name = "hybrid-pq"
description = "Isolated experimental hybrid post-quantum types and cryptographic trait boundaries."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
ml-dsa = { workspace = true, optional = true }
ml-kem = { workspace = true, optional = true }

[features]
default = []
experimental-pq-primitives = ["dep:ml-dsa", "dep:ml-kem"]

[dev-dependencies]
ciborium = { workspace = true }
crypto-traits = { path = "../crypto-traits" }
proptest = { workspace = true }

[lints]
workspace = true
