# Shared interoperability vectors

This directory is reserved for redacted, deterministic OpenID4VCI/OpenID4VP
vectors that can be replayed against both this wallet and the EU reference
implementation. Vectors must contain no personal data, private keys, or live
credentials. Each vector should identify its protocol profile, expected result,
and the source specification version.
